# Todo

- extract the entity name
  - take from the @Entity decorator options
  - if not present, take the class name and convert to snake case
- extract the column name
    - take from the @Column decorator options
    - if not present, take the property name 
- extract the column type
    - map the TypeORM types to the TypeScript types
    - if not present, take the property type
    - In case `nullable: true` is set, make the property type nullable
    - In case it's JSON, take the property type
    - In case it's an array, take the property type
    - In case it's an enum, take the property type
    - Column decorators:
          @Column,
          @PrimaryColumn,
          @PrimaryGeneratedColumn,
          @ObjectIdColumn,
          @CreateDateColumn,
          @UpdateDateColumn,
          @DeleteDateColumn,
          @VersionColumn,
          @Generated,
          @VirtualColumn,
    - In case the column is autogenerated, wrap the type in a Generated type from kysely
- 
